# Kernel config for x86-64 desktop platform.
# Refactored from base/kernel.config — see base/ for the current working config.
# This file will be synchronized with base/kernel.config when platform migration is complete.
