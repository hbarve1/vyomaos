[package]
name    = "check-manifests"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "check-manifests"
path = "src/main.rs"

[dependencies]
supervisor = { path = "../../supervisor" }

[dev-dependencies]
tempfile = "3"
