# VyomaOS kernel configuration
# Applied via: make KCONFIG_ALLCONFIG=base/kernel.config tinyconfig
# All options are built-in (=y). Loadable modules are disabled.

# ── Architecture ──────────────────────────────────────────────────────────────
CONFIG_64BIT=y
CONFIG_X86_64=y

# ── ELF / executable support ──────────────────────────────────────────────────
CONFIG_BINFMT_ELF=y
CONFIG_BINFMT_SCRIPT=y

# ── Initramfs ─────────────────────────────────────────────────────────────────
CONFIG_BLK_DEV_INITRD=y
CONFIG_RD_GZIP=y

# ── Core virtual filesystems ──────────────────────────────────────────────────
CONFIG_TMPFS=y
CONFIG_PROC_FS=y
CONFIG_SYSFS=y
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y

# ── Serial console (8250 UART — standard QEMU ISA serial) ────────────────────
CONFIG_TTY=y
CONFIG_SERIAL_8250=y
CONFIG_SERIAL_8250_CONSOLE=y
CONFIG_SERIAL_8250_NR_UARTS=4
CONFIG_SERIAL_8250_RUNTIME_UARTS=4

# ── Console / printk ─────────────────────────────────────────────────────────
CONFIG_PRINTK=y
CONFIG_EARLY_PRINTK=y

# ── Clock source ─────────────────────────────────────────────────────────────
CONFIG_HZ_100=y

# ── PCI bus (required for virtio-pci) ────────────────────────────────────────
CONFIG_PCI=y
CONFIG_PCI_MSI=y

# ── Block layer ───────────────────────────────────────────────────────────────
CONFIG_BLOCK=y

# ── Disable loadable modules ──────────────────────────────────────────────────
# CONFIG_MODULES is not set

# ── Networking base layer ─────────────────────────────────────────────────────
CONFIG_NET=y
CONFIG_INET=y
CONFIG_PACKET=y
CONFIG_UNIX=y

# ── Process management ───────────────────────────────────────────────────────
CONFIG_MULTIUSER=y
CONFIG_FUTEX=y
CONFIG_EPOLL=y
CONFIG_SIGNALFD=y
CONFIG_TIMERFD=y
CONFIG_EVENTFD=y

# ── Filesystem basics ────────────────────────────────────────────────────────
CONFIG_EXT4_FS=y

# ── Disable swap ─────────────────────────────────────────────────────────────
# CONFIG_SWAP is not set

# ── Security: disabled until P08 ─────────────────────────────────────────────
# CONFIG_AUDIT is not set
# CONFIG_IKCONFIG is not set

# ── VirtIO core ───────────────────────────────────────────────────────────────
CONFIG_VIRTIO=y
CONFIG_VIRTIO_MENU=y

# ── VirtIO transport: PCI ─────────────────────────────────────────────────────
CONFIG_VIRTIO_PCI=y
CONFIG_VIRTIO_PCI_LEGACY=y

# ── VirtIO block device (/dev/vda) ────────────────────────────────────────────
CONFIG_VIRTIO_BLK=y

# ── VirtIO network device ─────────────────────────────────────────────────────
CONFIG_VIRTIO_NET=y

# ── VirtIO RNG (getrandom() for Wasmtime startup) ────────────────────────────
CONFIG_HW_RANDOM=y
CONFIG_HW_RANDOM_VIRTIO=y

# ── 9P filesystem (host directory sharing via QEMU -virtfs) ──────────────────
CONFIG_NET_9P=y
CONFIG_NET_9P_VIRTIO=y
CONFIG_9P_FS=y
CONFIG_9P_FS_POSIX_ACL=y
